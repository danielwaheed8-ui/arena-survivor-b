CHILD-2 BRIEF — entities.js (player + input physics, enemies + AI + wave spawner + boss)

You are an autonomous Claude Code worker for the "Arena Survivor" 3D game.

STEP 0 — READ IN FULL FIRST:
  /Users/danielwaheed/experiment3/mechb/CONTRACT.md   (the shared module contract — OBEY IT EXACTLY)
Re-read the STATE object, Enemy shape, Event shapes, and the "entities.js (CHILD-2)"
export block. Your functions MUTATE the exact fields named there.

HARD RULES:
- Work ONLY inside /Users/danielwaheed/experiment3/mechb. Build EXACTLY ONE file:
    /Users/danielwaheed/experiment3/mechb/entities.js
  Do NOT create any other module (no render.js/combat.js/audio.js/index.html/main.js).
  Do NOT touch briefs/, logs/, CONTRACT.md, or anything outside the workdir.
- Coding tools only (read/write/edit + bash for `node` tests). NO network, NO servers, NO
  irreversible/external tools. Do NOT spawn sub-agents/Claude sessions. Do NOT use Workflow.
- entities.js MUST NOT import three / addons and MUST NOT reference window/document/THREE/
  AudioContext. PURE data+math over plain objects, so it runs under Node for testing.
- DETERMINISM: never call Math.random(). Use `state.rng()` (provided on state) for ALL
  randomness (spawn positions, etc.). Same inputs -> same mutations.

EXPORTS TO IMPLEMENT (exact names; see CONTRACT for full semantics):
  export const ARENA_HALF = 40;
  export function updatePlayer(state, dt, input)   // accel+friction movement, clamp to arena,
       // moveSpeed upgrade + speed buff, dash impulse, decrement player timers, update aim.
  export function updateEnemies(state, dt)         // steering toward player; per-type behavior;
       // integrate vel->pos; ramp spawnScale->1; decrement hitFlash; SHOOTER/BOSS fire ENEMY
       // projectiles into state.projectiles (fromPlayer:false, id=state.nextId++) aimed at player.
  export function startWave(state)                 // wave++, build roster (grows w/ wave; BOSS when
       // wave%5===0), set enemiesToSpawn/spawnTimer, waveActive=true, betweenWaves=false,
       // push {type:'waveStart',wave}.
  export function updateSpawner(state, dt)         // drip-spawn queued enemies at arena-edge
       // positions (state.rng); when none left to spawn AND no enemies remain ->
       // waveActive=false, betweenWaves=true.
  export function spawnEnemyAt(state, x, z, type='chaser')  // create+push+return one enemy.

MOVEMENT FEEL (required): vector accel toward desired direction, friction when no input,
NO instant snapping. Player base speed ~ 12 u/s scaled by (1 + 0.12*upgrades.moveSpeed) and
*1.5 while buffs.speed>0. Dash = brief velocity impulse along aim/move dir with a cooldown
(input.dash triggers it if dashCooldown<=0). Clamp pos to ±ARENA_HALF.

ENEMY BEHAVIOR (make types DISTINCT & deterministic):
- chaser: steer straight at player, fast.
- tank: slow, steers at player, high hp.
- shooter: approach to a preferred range (~18) then strafe/hold and fire enemy projectiles
  at the player on a cooldown (~1.5s); projectile speed ~14, damage from its `damage`.
- splitter: medium chaser (combat.js spawns 2 chasers when it dies — you just move it).
- boss: large, slow, steers at player, very high hp; may fire a spread of enemy projectiles
  on a longer cooldown.
Steering: compute desired velocity = normalize(player.pos - enemy.pos) * enemy.speed, then
move enemy.vel toward it (lerp/accel) and integrate. Keep enemies inside the arena.

WAVES: startWave builds a roster whose size/strength scales with wave number; mix chaser/
tank/shooter/splitter; every 5th wave (wave%5===0) is a BOSS wave (spawn 1 boss + a few
adds). updateSpawner releases enemies over time (spawnTimer countdown) at random points on
the arena edge, decrementing enemiesToSpawn. Set betweenWaves=true once the wave is fully
cleared so the orchestrator can show the upgrade screen and later call startWave again.

SELF-TEST (MANDATORY — actually execute, not just node --check): write a throwaway
`node --input-type=module` script (or a temp .mjs you delete after) that:
  - builds a minimal fake state: {time:0,score:0,health:100,maxHealth:100,wave:0,gameOver:false,
    player:{pos:{x:0,z:0},vel:{x:0,z:0},radius:0.8,fireCooldown:0,invuln:0,hitFlash:0,
    dashCooldown:0,aim:{x:0,z:1}}, enemies:[],projectiles:[],powerups:[],events:[],
    upgrades:{damage:0,fireRate:0,moveSpeed:0,maxHealth:0},buffs:{rapidFire:0,spread:0,speed:0},
    waveActive:false,betweenWaves:true,enemiesToSpawn:0,spawnTimer:0,rng:()=>0.5,nextId:1}
  - asserts: updatePlayer with input{moveX:1,moveZ:0} for several dt moves player +x and
    builds velocity then friction decays it when input is zero; pos stays within ±40.
  - spawnEnemyAt(state,10,0,'chaser') pushes an enemy with hp>0 and id set; updateEnemies
    moves it toward player (its distance to origin decreases over steps).
  - startWave(state) sets wave=1, waveActive=true, enemiesToSpawn>0, pushes a waveStart event;
    repeated updateSpawner(state, dt) eventually creates enemies.
  - a shooter at range fires an enemy projectile into state.projectiles after enough time.
  Print PASS lines; delete the temp file when green.

FINISH PROTOCOL (final actions, in order):
  1. Append at start: <UTC> | START | child-2 | building entities.js
     Append at end:   <UTC> | DONE | child-2 | entities.js built + node logic test green
     to /Users/danielwaheed/experiment3/mechb/PROGRESS.log  (<UTC> = date -u +%Y-%m-%dT%H:%M:%SZ)
  2. Write /Users/danielwaheed/experiment3/mechb/logs/child-2.done containing: done
Then stop.
