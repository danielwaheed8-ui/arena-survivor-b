"""Core engine primitives with no gameplay knowledge.

These modules (events, state machine, object pool) are deliberately generic so
they can be unit-tested in isolation and reused by any system.
"""
